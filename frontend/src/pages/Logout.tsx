import { Button } from "../components/Button";

const Logout = () => {
  return (
    <div>
      <Button to="/" type="submit">
        Logout
      </Button>
    </div>
  );
};

export default Logout;
